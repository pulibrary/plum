<% if current_search_session && current_search_session.query_params && current_search_session.query_params.include?('controller') %>
  <%= link_to t('blacklight.back_to_search'), Rails.application.routes.url_for(current_search_session.query_params.symbolize_keys.merge(format: nil)), class: 'btn btn-default back-to-search' %>
<% end %>
