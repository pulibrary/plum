<fieldset class="required">
  <legend>Required Information</legend>
  <%= f.input :source_metadata_identifier, label: 'Source Metadata ID' %>
  <%= check_box_tag :refresh_remote_metadata, 1, false %>
  <label for="refresh_metadata">Refresh metadata from PULFA/Voyager</label>
</fieldset>

<fieldset class="required">
  <%= f.input :title, as: :multi_value, readonly: f.object.source_metadata_identifier.present? %>
</fieldset>
