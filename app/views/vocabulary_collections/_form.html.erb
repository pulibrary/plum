<%= form_for(vocabulary_collection) do |f| %>
  <% if vocabulary_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vocabulary_collection.errors.count, "error") %> prohibited this vocabulary_collection from being saved:</h2>

      <ul>
      <% vocabulary_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %>
    <%= f.text_field :label %>
  </div>

  <div class="field">
    <%= f.label :vocabulary_id %>
    <%= f.collection_select :vocabulary_id, Vocabulary.order(:label), :id, :label, selected: @vocabulary_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
